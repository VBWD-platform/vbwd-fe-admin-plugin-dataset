{
  "debug_mode": {
    "type": "boolean",
    "default": false,
    "description": "Enable verbose datasets admin debug logging"
  },
  "datasetsPerPage": {
    "type": "number",
    "default": 20,
    "description": "Number of datasets per page in the admin list"
  }
}
